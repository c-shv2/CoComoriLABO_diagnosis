# 回答保存の有効化

1. 非公開の回答シートを開く。
2. 「拡張機能」→「Apps Script」を開く。
3. `Code.gs` の内容をすべて貼り付けて保存する。
4. 「デプロイ」→「新しいデプロイ」→「ウェブアプリ」を選ぶ。
5. 実行ユーザーは自分、アクセスできるユーザーは全員にしてデプロイする。
6. 発行された `/exec` のURLを `app.js` 冒頭の `RESPONSE_ENDPOINT` に設定する。

シート本体は共有せず、非公開のまま使用する。ウェブアプリのURLは回答を受け取る入口で、閲覧用URLではない。
